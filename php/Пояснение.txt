Задание 1:
        В методе filter_insert_data была добавлена проверка: если
        в массиве данных есть значение, то значение по умолчанию не
        присваивается.
        Что бы проверить работоспособность необходимо запустить файл
        test.php с раскомменированной секцией 1.

Задание 2.1:
        Для решения задачи 2.1 был описан метод filter_data в db_section.inc,
        который фильтрует данные.
        Что бы проверить работоспособность необходимо в файле test.php
        закомментировать секцию 1 и расскомментировать секциию 2.1

Задание 2.2:
        Для решения задачи 2.2 были описаны методы filter_insert_data
        и filter_update_data в  db_section.inc. Данные фунции фильтруют данные и
        проверяет, существует ли сайт с ID, к которому добавляется или обновляется
        site-section. Так же фуннкция filter_update_data, проверяет существует
        ли db_section c заданным ID.
        Что бы проверить работоспособность необходимо в файле test.php
        закомментировать секцию 2.1 и расскомментировать секцию 2.2.
        В массиве $section1_data необходимо указать ID существующего
        в базе site в site_id. Если будет указан не существующий site_id будет
        выдана ошибка.
        Для проверки filter_update_data, закомментировать секцию 2.1 и раскомментировать
        секцию 2.2,а так же в массиве $section2_data необходимо указать ID существующей
        в базе section и существующий site_id в базе site. В противном случае будет выдана ошибка.

Задание 3.
        Для решения задачи 3 был описан метод check_double_data в dbobject.inc.
        Данный метод проверяет есть ли одинаковы значения переданные в массиве данных
        и в таблице.
        Если есть, будет сгенерирована ошибка.
        Для проверки метода check_double_data необходимо закомментировать секцию
        2.2 и раскомментировать секцию 3 в файле test.php.

Задание 4.
        Для решения задачи 4 был описан метод check_type_data  в dbobject.inc.
        Если входные данные и тип в описании не совпадает, то значению присваивается NULL.
        Для проверки метода check_type_data необходимо закомментировать секцию 3 и
        раскомментировать секцию 4 в файле test.php.





